---
interface Props {
  slot?: string;
  label?: string;
}

const { slot = 'default', label = 'Advertisement' } = Astro.props;
---
<aside class="ad-banner" data-ad-slot={slot} aria-label={label}></aside>
